const person1={
  fname:"john",
  lname:"doe",
  age:30,
  country:"USA"
}

let intro='congrtulaions ' +person1.fname+' ' +person1.lname +'you are turning to ' +person1.age;
console.log(intro);

let a = 5;
let b = 10;
console.log(`The sum of ${a} and ${b} is ${a+b}`);

const person = {
    firstName: 'John',
    lastName: 'Doe',
    age: 20,
    city: 'London'
}

let info = `Name:   ${person.firstName} ${person.lastName}
Age:    ${person.age}
City:   ${person.city}`;

console.log(info);
